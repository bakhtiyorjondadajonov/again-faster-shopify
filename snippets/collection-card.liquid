<!-- {%assign collection = collection_data%} -->
<div class="collection-card">
    <div class="collection-card__img-box">
        <img src="{{collection_data.image.src | img_url:"1920x"}}" alt="">
    </div>
    <a href="{{collection_data.link}}" class="collection-card__title">
        <span>{{collection_data.title}}</span>
        <span class="arrow"><img src="{{"arrow-long-black.png" | asset_url}}" alt=""></span>
    </a>
</div>

<style>
    .collection-card {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        width: 100%;
        height: 100%;
        justify-content: center;
        align-items: center;
    }
    .collection-card__img-box {
        height: 80%;
        width: 100%;
    }
    .collection-card__title {
        height: 3rem;
        display: flex;
        gap: 1rem;
        align-items: center;
        justify-items: center;
        font-family: var(--secondary-font);
        font-size: 12px;
        font-weight: 300;
        line-height: 20px;
        letter-spacing: 0.16em;
        text-align: center;
        text-transform: uppercase;
        cursor: pointer;
        transition: all 0.5s;
    }

    .collection-card__title:hover .arrow {
        opacity: 1;
    }
</style>